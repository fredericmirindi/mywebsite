module.exports = {
    content: {
          hovers,: ['' ]
         },
        folder: ['all(sql)', 'name(header', 'top', 'bottom', 'menu', 'tailed', 'body'],
        require: (require('postcss-import')(), () => require('tailwindcss/con') )
    }
