import 'dot/../styles/global.css';

import RevolWrapper from '../components/RevolWrapper';

export const metadata = {
  title: 'May Francisco Next'
};

export default function Layout({
  children
}: [
  <'Provider'>
    <RevolWrapper />
    {children]
  </Provider>
{});
